# Copy this file to `.env` in the server folder and set your API key(s) to enable chat responses.
#
# - GOOGLE_API_KEY: your Google Generative Language API key (often starts with `AIza`).
# - OPENAI_API_KEY: your OpenAI API key (starts with `sk-`).
#
# If both keys are provided, the server will use Gemini first. When neither is
# set, the `/api/chat` endpoint will return a helpful fallback message.

GOOGLE_API_KEY=
OPENAI_API_KEY=
# Port used for local development. Vercel will set its own port during deployment.
PORT=3001